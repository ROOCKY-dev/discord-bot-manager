// Discord Bot Commands Handler\n// This module handles all Discord bot commands\n\nexport const COMMANDS = {\n  // Moderation Commands\n  ban: \"Ban a user from the server\",\n  kick: \"Kick a user from the server\",\n  warn: \"Warn a user\",\n  mute: \"Mute a user\",\n  \n  // Economy Commands\n  balance: \"Check your balance\",\n  daily: \"Claim your daily reward\",\n  leaderboard: \"Show the economy leaderboard\",\n  \n  // Utility Commands\n  help: \"Show available commands\",\n  ping: \"Check bot latency\",\n};\n\nexport function parseCommand(content: string, prefix: string) {\n  if (!content.startsWith(prefix)) return null;\n  \n  const args = content.slice(prefix.length).trim().split(/ +/);\n  const command = args.shift()?.toLowerCase();\n  \n  return { command, args };\n}\n\nexport function isValidCommand(command: string | undefined): boolean {\n  return command ? command in COMMANDS : false;\n}\n\nexport function getCommandHelp(command: string): string | undefined {\n  return COMMANDS[command as keyof typeof COMMANDS];\n}\n\nexport function getAllCommands(): string[] {\n  return Object.keys(COMMANDS);\n}\n"
